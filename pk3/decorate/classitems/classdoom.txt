actor JKEChainsawPickup: JKEClassPickupSpawner replaces Chainsaw
{
    +NOINTERACTION

    States
    {
        Spawn:
          TNT1 A 0
          TNT1 A 0 A_JumpIf(ACS_ExecuteWithResult(684) == 1, "DropSpawn")
          goto NormSpawn

        DropSpawn:
          TNT1 A 0 A_SpawnItemEx("JKEClass1Drop", 0, 0, 0, momx, momy, momz)
          stop

        NormSpawn:
          TNT1 A 0 A_SpawnItemEx("JKEClass1Pickup", 0, 0, 0, momx, momy, momz)
          stop
    }
}

actor JKEPistolPickup: JKEClassPickupSpawner replaces Pistol
{
    +NOINTERACTION

    States
    {
        Spawn:
          TNT1 A 0
          TNT1 A 0 A_JumpIf(ACS_ExecuteWithResult(684) == 1, "DropSpawn")
          goto NormSpawn

        DropSpawn:
          TNT1 A 0 A_SpawnItemEx("JKEClass2Drop", 0, 0, 0, momx, momy, momz)
          stop

        NormSpawn:
          TNT1 A 0 A_SpawnItemEx("JKEClass2Pickup", 0, 0, 0, momx, momy, momz)
          stop
    }
}

actor JKEShotgunPickup: JKEClassPickupSpawner replaces Shotgun
{
    +NOINTERACTION

    States
    {
        Spawn:
          TNT1 A 0
          TNT1 A 0 A_JumpIf(ACS_ExecuteWithResult(684) == 1, "DropSpawn")
          goto NormSpawn

        DropSpawn:
          TNT1 A 0 A_SpawnItemEx("JKEClass3Drop", 0, 0, 0, momx, momy, momz)
          stop

        NormSpawn:
          TNT1 A 0 A_SpawnItemEx("JKEClass3Pickup", 0, 0, 0, momx, momy, momz)
          stop
    }
}

actor JKESuperShotgunPickup: JKEClassPickupSpawner replaces SuperShotgun
{
    +NOINTERACTION

    States
    {
        Spawn:
          TNT1 A 0
          TNT1 A 0 A_JumpIf(ACS_ExecuteWithResult(684) == 1, "DropSpawn")
          goto NormSpawn

        DropSpawn:
          TNT1 A 0 A_SpawnItemEx("JKEClass3Drop2", 0, 0, 0, momx, momy, momz)
          stop

        NormSpawn:
          TNT1 A 0 A_SpawnItemEx("JKEClass3Pickup2", 0, 0, 0, momx, momy, momz)
          stop
    }
}

actor JKEChaingunPickup: JKEClassPickupSpawner replaces Chaingun
{
    +NOINTERACTION

    States
    {
        Spawn:
          TNT1 A 0
          TNT1 A 0 A_JumpIf(ACS_ExecuteWithResult(684) == 1, "DropSpawn")
          goto NormSpawn

        DropSpawn:
          TNT1 A 0 A_SpawnItemEx("JKEClass4Drop", 0, 0, 0, momx, momy, momz)
          stop

        NormSpawn:
          TNT1 A 0 A_SpawnItemEx("JKEClass4Pickup", 0, 0, 0, momx, momy, momz)
          stop
    }
}

actor JKEMinigunPickup: JKEClassPickupSpawner replaces Minigun
{
    +NOINTERACTION

    States
    {
        Spawn:
          TNT1 A 0
          TNT1 A 0 A_JumpIf(ACS_ExecuteWithResult(684) == 1, "DropSpawn")
          goto NormSpawn

        DropSpawn:
          TNT1 A 0 A_SpawnItemEx("JKEClass4Drop2", 0, 0, 0, momx, momy, momz)
          stop

        NormSpawn:
          TNT1 A 0 A_SpawnItemEx("JKEClass4Pickup2", 0, 0, 0, momx, momy, momz)
          stop
    }
}

actor JKERocketLauncherPickup: JKEClassPickupSpawner replaces RocketLauncher
{
    +NOINTERACTION

    States
    {
        Spawn:
          TNT1 A 0
          TNT1 A 0 A_JumpIf(ACS_ExecuteWithResult(684) == 1, "DropSpawn")
          goto NormSpawn

        DropSpawn:
          TNT1 A 0 A_SpawnItemEx("JKEClass5Drop", 0, 0, 0, momx, momy, momz)
          stop

        NormSpawn:
          TNT1 A 0 A_SpawnItemEx("JKEClass5Pickup", 0, 0, 0, momx, momy, momz)
          stop
    }
}

actor JKEGrenadeLauncherPickup: JKEClassPickupSpawner replaces GrenadeLauncher
{
    +NOINTERACTION

    States
    {
        Spawn:
          TNT1 A 0
          TNT1 A 0 A_JumpIf(ACS_ExecuteWithResult(684) == 1, "DropSpawn")
          goto NormSpawn

        DropSpawn:
          TNT1 A 0 A_SpawnItemEx("JKEClass5Drop2", 0, 0, 0, momx, momy, momz)
          stop

        NormSpawn:
          TNT1 A 0 A_SpawnItemEx("JKEClass5Pickup2", 0, 0, 0, momx, momy, momz)
          stop
    }
}

actor JKEPlasmaRiflePickup: JKEClassPickupSpawner replaces PlasmaRifle
{
    +NOINTERACTION

    States
    {
        Spawn:
          TNT1 A 0
          TNT1 A 0 A_JumpIf(ACS_ExecuteWithResult(684) == 1, "DropSpawn")
          goto NormSpawn

        DropSpawn:
          TNT1 A 0 A_SpawnItemEx("JKEClass6Drop", 0, 0, 0, momx, momy, momz)
          stop

        NormSpawn:
          TNT1 A 0 A_SpawnItemEx("JKEClass6Pickup", 0, 0, 0, momx, momy, momz)
          stop
    }
}

actor JKERailgunPickup: JKEClassPickupSpawner replaces Railgun
{
    +NOINTERACTION

    States
    {
        Spawn:
          TNT1 A 0
          TNT1 A 0 A_JumpIf(ACS_ExecuteWithResult(684) == 1, "DropSpawn")
          goto NormSpawn

        DropSpawn:
          TNT1 A 0 A_SpawnItemEx("JKEClass6Drop2", 0, 0, 0, momx, momy, momz)
          stop

        NormSpawn:
          TNT1 A 0 A_SpawnItemEx("JKEClass6Pickup2", 0, 0, 0, momx, momy, momz)
          stop
    }
}

actor JKEBFG9000Pickup: JKEClassPickupSpawner replaces BFG9000
{
    +NOINTERACTION

    States
    {
        Spawn:
          TNT1 A 0
          TNT1 A 0 A_JumpIf(ACS_ExecuteWithResult(684) == 1, "DropSpawn")
          goto NormSpawn

        DropSpawn:
          TNT1 A 0 A_SpawnItemEx("JKEClass7Drop", 0, 0, 0, momx, momy, momz)
          stop

        NormSpawn:
          TNT1 A 0 A_SpawnItemEx("JKEClass7Pickup", 0, 0, 0, momx, momy, momz)
          stop
    }
}

actor JKEBFG10kPickup: JKEClassPickupSpawner replaces BFG10k
{
    +NOINTERACTION

    States
    {
        Spawn:
          TNT1 A 0
          TNT1 A 0 A_JumpIf(ACS_ExecuteWithResult(684) == 1, "DropSpawn")
          goto NormSpawn

        DropSpawn:
          TNT1 A 0 A_SpawnItemEx("JKEClass8Drop", 0, 0, 0, momx, momy, momz)
          stop

        NormSpawn:
          TNT1 A 0 A_SpawnItemEx("JKEClass8Pickup", 0, 0, 0, momx, momy, momz)
          stop
    }
}

